# Revised tutorial content audit

The updated script contains **999 spoken words across 32 scenes**. The **46-item** coverage checklist includes the new requirement that each completed principal and secondary word contain a vowel and a consonant, counting existing letters. Y remains a vowel.

The `setup-board` narration explains grey opening tiles and green/orange player contributions. `secondary-words` states the new word-composition rule. `score-recap` removes the obsolete all-vowel, zero-point claim. These three scenes have regenerated Microsoft Zira narration at Rate 1, with native 16 kHz audio and word-progress timestamps. All other narration is unchanged.

Production-engine and exact shipped-dictionary checks pass for all five examples: **186, 147, 261, 42 and 171**. Every completed illustrated word contains both letter types. The diagram renderer retains its explicit existing/new legend, pillars, numbered spans and scoring labels rather than inventing player ownership for constructed positions. MASTERPIECE inference and all 26 tile values and quantities are checked against the application sources.

The renderer passes TypeScript checking. The offline viewer now derives all chapter positions, duration and word-count text from Scene-manifest.json. Its generator was checked against a supplied manifest; complete browser checks run after the revised video export.

Final timing, caption alignment, frame layout, application footage, complete playback and packaging checks are recorded separately in the regenerated caption-qa.json, Verification.json, Browser-playback-verification.json, Viewer-verification.json and Production-report.md. This content audit does not certify pronunciation through subjective listening.
